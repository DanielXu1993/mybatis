<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xyh.model.mapper.CountryMapper">
    <!--
        resultType:当前查询的返回值类型.前面已经设置了别名
    -->
    <select id="selectAll" resultType="com.xyh.model.Country">
        select id,countryname,countrycode from country
    </select>
</mapper>